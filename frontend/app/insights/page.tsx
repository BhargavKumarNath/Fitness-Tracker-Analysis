import { DataQualityPanel } from "@/components/DataQualityPanel";
import { dataQualityData } from "@/lib/data";
export default function InsightsPage() { const findings = dataQualityData.findings; return <Page title="Insights" eyebrow="03 / Data quality" detail="A transparent account of the dataset's shape, completeness, and unusual values."><DataQualityPanel data={dataQualityData} /><section className="mt-16 border-t border-line pt-10"><p className="font-mono text-xs uppercase tracking-[0.18em] text-cobalt">Key findings</p><div className="mt-6 grid gap-4 md:grid-cols-3"><article className="border border-line p-5"><p className="text-sm text-muted">Most common activity</p><p className="mt-8 font-display text-2xl capitalize">{findings.mostPopularActivity.replaceAll("_", " ")}</p></article><article className="border border-line p-5"><p className="text-sm text-muted">High performers</p><p className="mt-8 font-display text-2xl">{findings.highPerformerPercentage.toFixed(1)}%</p><p className="mt-2 text-xs text-muted">above 10,000 steps</p></article><article className="border border-line p-5"><p className="text-sm text-muted">Most efficient activity</p><p className="mt-8 font-display text-2xl capitalize">{findings.mostEfficientActivity.replaceAll("_", " ")}</p><p className="mt-2 text-xs text-muted">calories per step</p></article></div></section></Page>; }
function Page({ title, eyebrow, detail, children }: { title: string; eyebrow: string; detail: string; children: React.ReactNode }) { return <div className="mx-auto max-w-[1440px] px-5 py-14 md:px-10 md:py-24"><p className="font-mono text-xs uppercase tracking-[0.18em] text-cobalt">{eyebrow}</p><h1 className="mt-5 font-display text-5xl tracking-[-0.04em] md:text-7xl">{title}</h1><p className="mt-5 max-w-xl text-lg leading-8 text-muted">{detail}</p><div className="mt-14">{children}</div></div>; }