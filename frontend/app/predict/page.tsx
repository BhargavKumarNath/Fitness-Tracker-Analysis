import { PredictionForm } from "@/components/PredictionForm";
import activityAnalysis from "@/public/data/activity_analysis.json";
import type { ActivityAnalysisArtifact } from "@/lib/types";
export default function PredictPage() { const data = activityAnalysis as ActivityAnalysisArtifact; return <Page title="Predict" eyebrow="05 / Instant inference" detail="Client-side baseline estimates with no model download, server request, or cold start."><div className="mb-8 border-l-2 border-cobalt pl-5 text-sm leading-7 text-muted">These are fast estimates matching the dashboard&apos;s default fallback path. They are not presented as the optional trained Random Forest artifacts.</div><PredictionForm categories={data.categories} /></Page>; }
function Page({ title, eyebrow, detail, children }: { title: string; eyebrow: string; detail: string; children: React.ReactNode }) { return <div className="mx-auto max-w-[1440px] px-5 py-14 md:px-10 md:py-24"><p className="font-mono text-xs uppercase tracking-[0.18em] text-cobalt">{eyebrow}</p><h1 className="mt-5 font-display text-5xl tracking-[-0.04em] md:text-7xl">{title}</h1><p className="mt-5 max-w-xl text-lg leading-8 text-muted">{detail}</p><div className="mt-14">{children}</div></div>; }