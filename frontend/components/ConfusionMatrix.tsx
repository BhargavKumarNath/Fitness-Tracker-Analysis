export function ConfusionMatrix({ labels, matrix }: { labels: string[]; matrix: number[][] }) {
  return <div className="overflow-x-auto"><table className="border-collapse text-left text-sm"><caption className="mb-3 text-left text-xs text-muted">Rows are actual labels; columns are predicted labels.</caption><thead><tr><th className="border-b border-line p-3 font-normal text-muted">Actual / predicted</th>{labels.map((label) => <th key={label} className="border-b border-line p-3 font-normal capitalize">{label.replaceAll("_", " ")}</th>)}</tr></thead><tbody>{matrix.map((row, rowIndex) => <tr key={labels[rowIndex]}><th className="border-b border-line p-3 font-normal capitalize">{labels[rowIndex].replaceAll("_", " ")}</th>{row.map((value, columnIndex) => <td key={`${rowIndex}-${columnIndex}`} className="border-b border-line p-3 text-center font-mono" style={{ background: `rgba(23, 70, 162, ${Math.min(0.7, value / Math.max(1, Math.max(...row)) * 0.7)})`, color: value > Math.max(...row) * 0.45 ? "white" : "#171717" }}>{value}</td>)}</tr>)}</tbody></table></div>;
}